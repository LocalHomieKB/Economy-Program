#State is used like normal and is capable of disabling it.
# instate is used to check.
# 
# But the entry flage "redonly" can be used to allow selection of text and copy of it

# There is also an invalid state, which where the widget fails validation...