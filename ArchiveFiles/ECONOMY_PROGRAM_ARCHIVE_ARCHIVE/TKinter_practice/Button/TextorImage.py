#Buttons take text, textvariable, image and compound config options
# 
# Buttons have a default config option, if specfied as 
#"active" his means it is the default button in the user
#interface. Or it is "normal".
# Default buttons are invokled if return or enter
# is pressed, as long as you set the event binding this way